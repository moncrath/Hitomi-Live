# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/

# Tauri / Rust
src-tauri/target/

# Catatan: `Hitomi Live.exe` di root SENGAJA di-commit supaya bisa diunduh langsung
# dari GitHub tanpa build. Konsekuensinya tiap rebuild menambah ~15MB permanen ke
# history repo (tak bisa dihapus tanpa menulis ulang history).

# Env & secrets
.env
.env.*
!.env.example

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code local settings (path mesin, jangan di-commit)
.claude/settings.local.json

# Config MCP lokal (server per-mesin, mis. Playwright)
.mcp.json

# Artefak Playwright MCP (screenshot/log uji)
.playwright-mcp/
